{
  "buildCommand": "cd main-web-app && npm install && npm run build",
  "outputDirectory": "main-web-app/dist",
  "ignoreCommand": "false",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
